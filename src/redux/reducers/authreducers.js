import types from '../types';
const INITIAL_STATE = {
  
};
const authreducers = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    

    

    default:
      return state;
  }
};

export default authreducers;
